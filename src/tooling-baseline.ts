/**
 * Describe the default tooling baseline shipped by this template.
 *
 * @returns The baseline label used by smoke tests and generated docs.
 */
export function describeToolingBaseline(): string {
    return "Nick2bad4u TypeScript/npm repository tooling baseline";
}
